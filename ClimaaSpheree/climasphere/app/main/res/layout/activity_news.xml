<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#CBE5FF"
    android:padding="16dp">

    <!-- 🔙 Back Button -->
    <LinearLayout
        android:id="@+id/btnBackDashboard"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:clickable="true"
        android:focusable="true"
        android:padding="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="12dp"
        android:contentDescription="Go back to Dashboard">   <!-- ★ Added for accessibility -->

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Back" /> <!-- ★ Optional -->
    </LinearLayout>

    <!-- 📰 Header -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🌦️ Weather News"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/black" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Latest updates on weather and climate"
        android:textColor="@color/dark_gray"
        android:textSize="14sp"
        android:layout_marginBottom="12dp" />

    <!-- 🔍 Search Bar -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchInputLayout"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/bg_search_field_news"
        android:queryHint="Search weather news..."
        android:iconifiedByDefault="false"
        android:paddingHorizontal="12dp"
        android:focusable="true"
        android:focusableInTouchMode="true" />

    <!-- ⏳ Progress Bar -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:visibility="gone"
        android:indeterminateTint="@color/black"
        android:contentDescription="Loading news" /> <!-- ★ Added -->

    <!-- 🗞️ News List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerNews"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        android:contentDescription="Weather news articles list" /> <!-- ★ Added -->

</LinearLayout>
